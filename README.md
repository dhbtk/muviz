to do:

for the debug ui:
- playback controls
- file browser
- make the playhead stick to the right side
- improve ui labels

- batch mode to get data from a bunch of songs to improve normalization (ie instead of normalizing within a single track, normalize to a given range we'll figure out)
